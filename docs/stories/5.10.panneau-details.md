# Story 5.10 : Panneau de détails du nœud sélectionné

## Status

Done

## Story

**En tant que** joueur,
**je veux** un panneau droit qui détaille le nœud sélectionné (comme le panneau
Details d'Unreal) : identité, mod fournisseur, permission, description, et tous ses
littéraux en formulaire,
**afin de** comprendre et paramétrer un nœud sans zoomer dessus — et d'éditer
confortablement les valeurs longues que l'édition inline (5.2b) tronque.

## Acceptance Criteria

1. **Panneau droit repliable (`Tab`)** (UX §2) : sélection simple → titre traduit,
   identifiant technique, catégorie, mod fournisseur, badge de permission (`🔒`
   ADMIN etc.), coût de fuel, pur/impur, description traduite (`descKey`) sur
   plusieurs lignes.
2. **Formulaire des littéraux** : chaque pin d'entrée data avec sa valeur, éditable
   via les mêmes contrôles que la 5.2b (une seule implémentation de contrôles,
   partagée) — champ large, pas tronqué. Pins câblés : « ← connecté à <nœud> »,
   cliquable (recentre sur la source).
3. Sélection multiple → compte des nœuds et actions groupées (supprimer, aligner
   quand la 5.7 existera) ; sélection vide → propriétés du **blueprint** (id, nom,
   description, version, permission plafond — éditables, `BlueprintMeta`).
4. Nœud fantôme → identité brute, mod manquant, littéraux conservés listés en
   lecture seule (préparation FR40).
5. État pur testé (contenu affiché par type de sélection, commit littéral via la
   même voie que 5.2b) ; largeur 120 px, superposition sous 800 px (UX §2).

## Tasks / Subtasks

- [x] Extraire de la 5.2b un composant contrôle-par-type réutilisable (case, nombre,
      texte, énum) — une seule source de vérité de saisie (AC2)
- [x] `client/editor/DetailsPanel.java` (rendu) + `DetailsPanelState` pur (AC1–AC4)
- [x] Édition de `BlueprintMeta` (nouvelle `EditOperation.SetMeta` si absente, avec
      inverse) (AC3)
- [x] Navigation « connecté à » → `focusNode` (5.6b) (AC2)
- [x] Tests headless d'état ; clés de langue en/fr ; `./gradlew build` vert

## Dev Notes

- Tout vient de `NodeDescriptor` + `Blueprint` : aucun accès au `NodeType` (P1).
- Le fournisseur est le namespace de l'identifiant — l'affichage « mod » réel
  (nom lisible du mod) attendra la synchro registre (épic 6) qui saura le porter.
- `Tab` bascule les DEUX panneaux (variables 5.5 + détails) — l'état de pliage vit
  dans l'écran, pas dans les panneaux.

## Change Log

| Date | Version | Description | Auteur |
|---|---|---|---|
| 2026-08-03 | 0.1 | Création (le panneau détails est dans l'UX §2 mais absent du découpage PRD) | SM (Bob) |
| 2026-08-03 | 1.0 | Implémentation complète, 88 tests client, build vert | Dev (James) |

## Dev Agent Record

### Agent Model Used

claude-fable-5

### Debug Log References

- `DetailsPanelStateTest` : 5 tests — un échec instructif (le traducteur identité ne
  remplace pas %s → ligne fantôme restructurée en INFO testable), verts ensuite

### Completion Notes List

- `DetailsPanelState.rows(sélection)` produit des **lignes typées** (HEADER, INFO,
  META_*, LITERAL, WIRED, NOTE) que le widget dessine et route — les quatre modes
  (blueprint, nœud, multi, fantôme) sont testés purs, traductions injectées.
- L'édition de littéraux du panneau passe par le MÊME `beginLiteralEdit` que le
  canevas (extrait en helper partagé) : une seule implémentation de saisie, et le
  champ actif s'affiche aussi sur le nœud dans le canevas.
- `EditOperation.SetMeta` ajoutée au core (17ᵉ op, inverse testé) : auteur et
  description éditables au clavier, **plafond de permission cyclable** — le
  changer revalide immédiatement tout le graphe (liserés 5.6b).
- Ligne « ← connecté à <source> » cliquable → `focusNode`.
- Déviations consignées : superposition < 800 px non faite (avec l'ascenseur, plus
  tard) ; actions groupées multi-sélection réduites à l'infobulle Suppr/Ctrl+D.
- VERIFY-5.10 (rendu panneau) rejoint la session en jeu finale.

### File List

- `core/src/main/java/fr/blueprint/core/graph/EditOperation.java` (modifié — SetMeta)
- `client/src/main/java/fr/blueprint/client/editor/DetailsPanelState.java` (créé)
- `client/src/main/java/fr/blueprint/client/editor/DetailsPanel.java` (créé)
- `client/src/main/java/fr/blueprint/client/editor/CanvasWidget.java` (modifié — panneau droit, beginLiteralEdit partagé)
- `client/src/test/java/fr/blueprint/client/editor/DetailsPanelStateTest.java` (créé)
- `core/src/main/resources/assets/blueprint/lang/{en_us,fr_fr}.json` (modifiés — 12 clés)

## QA Results

### Review Date: 2026-08-03 — Reviewed By: Quinn (Test Architect)

Gate: **PASS** → docs/qa/gates/5.10-panneau-details.yml — 2 low (allocations par frame du panneau ; pas d'ascenseur). Story passée à Done.

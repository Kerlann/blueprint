# Story 5.5 : Panneau des variables et nœuds Get/Set

## Status

Draft

## Story

**En tant que** joueur,
**je veux** créer et gérer des variables typées dans un panneau latéral et les glisser
dans le canevas en nœuds Get/Set, comme dans Unreal,
**afin de** faire circuler de l'état dans mes graphes — le modèle (`Variable`,
`VarScope`) et la VM (instructions lecture/écriture) existent, il manque l'interface
ET les nœuds.

## Acceptance Criteria

1. **Panneau gauche repliable (`Tab`)** listant les variables par portée (UX §2, §7),
   pastille de couleur du type : `+` crée (nom unique, type choisi, portée),
   double-clic renomme, menu contextuel retype / change la portée / supprime /
   fixe la valeur par défaut (contrôles de la 5.2b réutilisés). (AC1 PRD 5.5)
2. Toutes les mutations passent par les `EditOperation` de variables existantes
   (ou ajoutées si manquantes — vérifier les 16 ops), inverses dans l'`UndoStack`.
3. **Glisser une variable dans le canevas crée un nœud `Get`** ; **Ctrl+glisser crée
   `Set`** (exec in/out + entrée valeur). (AC2 PRD 5.5)
4. **Nœuds Get/Set réels** : `blueprint:var/get` et `blueprint:var/set` paramétrés
   par le nom de variable (littéral `string` figé), sorties/entrées typées par la
   variable ; le compilateur les traduit vers les instructions variable existantes.
   Un graphe Get/Set s'exécute headless de bout en bout (test VM).
5. **Retypage incompatible** : confirmation listant les liens qui seront invalidés
   avant d'appliquer, annulable (FR10, U2). (AC3 PRD 5.5)
6. Variable non utilisée : point discret dans le panneau, pas d'avertissement (UX §7).

## Tasks / Subtasks

- [ ] Inventaire des `EditOperation` variables (create/rename/retype/scope/default/
      delete) — compléter côté `core/graph` si besoin, avec inverses et tests (AC2)
- [ ] `core/nodes/` : `var/get` + `var/set` (ou lowering direct dans le compilateur —
      décision d'architecture à consigner) + test VM headless (AC4)
- [ ] `client/editor/VariablePanel.java` (rendu, drag source) + état pur
      `VariablePanelState` (sélection, renommage, portées) (AC1)
- [ ] Glisser-déposer panneau→canevas : `Get`/`Set` à la position de dépôt, accroché ;
      `Ctrl` bascule (AC3)
- [ ] Retypage : calcul des liens invalidés (via `canLink` sur chaque lien existant
      avec le nouveau type) + écran de confirmation (AC5)
- [ ] Tests headless : ops variables, création Get/Set câblés, liste des liens
      invalidés exacte (AC2, AC4, AC5)
- [ ] Clés de langue en/fr (panneau, confirmation) ; `./gradlew build` vert

## Dev Notes

- `Instruction` porte déjà « lit une variable vers un slot » / « écrit un slot dans
  une variable » (`LOCAL` dans l'exécution, le reste dans le `VarStore`) — vérifier
  comment le compilateur les émet aujourd'hui : si c'est déjà un lowering de nœuds
  `var/*`, l'AC4 est petit ; sinon c'est le gros de la story côté core.
- ⚠ Cette story touche `core` (nœuds, ops) : vérifier ce que fait la session
  parallèle avant de commencer, ou la faire en worktree.
- Le drag panneau→canevas traverse deux systèmes de coordonnées : le panneau est en
  écran, le dépôt en monde — passer par la caméra du canevas.
- Largeur du panneau : 96 px repliable ; sous 800 px de large, superposition (UX §2).

## Change Log

| Date | Version | Description | Auteur |
|---|---|---|---|
| 2026-08-03 | 0.1 | Création (inclut les nœuds var/get-set côté core, absents du découpage PRD) | SM (Bob) |

## Dev Agent Record

### Agent Model Used

### Completion Notes List

### File List

## QA Results

# Story 5.11 : Vue script — le graphe en texte

## Status

Done

## Story

**En tant que** joueur,
**je veux** basculer sur une vue partagée graphe | BScript avec sélection
synchronisée, copier le script, l'exporter, ou en importer un,
**afin de** lire mon blueprint en texte, le versionner, le partager — U6. Le
générateur et le parseur (épic 4) sont complets et testés : cette story n'est que de
l'interface.

## Acceptance Criteria

1. Le bouton **`Script`** (barre 5.6b) bascule en vue partagée : canevas à gauche,
   panneau BScript à droite (50/50, séparateur glissable) ; re-bascule = plein
   canevas. (UX §9)
2. Le texte est **régénéré débouncé** après chaque mutation (même mécanique que la
   validation 5.6b) ; coloration syntaxique minimale (mots-clés, identifiants,
   littéraux, commentaires) et numéros de ligne.
3. **Sélection synchronisée** : sélectionner un nœud surligne son bloc dans le
   texte et l'y fait défiler ; cliquer un bloc dans le texte sélectionne et
   recentre le nœud. (UX §9 — v1.0 lecture seule, l'édition texte re-parsée est
   explicitement v1.1)
4. **Copier** (tout le script vers le presse-papier), **Exporter** (fichier
   `config/blueprint/exports/<id>.bp`, même chemin que `/blueprint export`),
   **Importer** (remplace le graphe après confirmation U2, une entrée d'annulation).
5. La correspondance nœud↔plage de texte est fournie par le générateur (positions
   par nœud) ou reconstruite de façon testée ; état pur testé headless (bascule,
   correspondance, import = restauration annulable).

## Tasks / Subtasks

- [x] `ScriptGenerator` : exposer la correspondance nœud→plage de lignes si absente
      (extension `core/script`, coordonner avec la session parallèle) (AC3, AC5)
- [x] `client/editor/ScriptView.java` (rendu, défilement, séparateur) + état pur
      (texte courant, surlignages, correspondances) (AC1–AC3)
- [x] Copier/Exporter/Importer : presse-papier système, fichiers `.bp` (réutiliser
      le chemin d'export 4.4a), import = `ScriptParser` + remplacement annulable (AC4)
- [x] Coloration : lexèmes du `Lexer` existant si réutilisable, sinon règles simples
      testées (AC2)
- [x] Tests headless ; clés de langue en/fr ; `./gradlew build` vert

## Dev Notes

- `ScriptGenerator.generate` → `Result(text, issues)` : afficher aussi les
  `issues` (nœuds inconnus etc.) en tête de panneau.
- L'import qui remplace tout : passer par la reconstruction en `EditOperation` comme
  le collage 5.8 (même moteur) pour rester dans l'`UndoStack`.
- Le débouncé régénère sur le thread client — mesurer sur 500 nœuds ; si > 2 ms,
  générer hors frame (tâche `Minecraft.schedule` du tick suivant).

## Change Log

| Date | Version | Description | Auteur |
|---|---|---|---|
| 2026-08-03 | 0.1 | Création (UX §9 ; dépend de 5.6b pour la barre et de 5.8 pour le moteur de réémission) | SM (Bob) |
| 2026-08-03 | 1.0 | Implémentation complète, 106 tests client, build vert | Dev (James) |

## Dev Agent Record

### Agent Model Used

claude-fable-5

### Debug Log References

- `ScriptViewStateTest` : 5 tests — un échec instructif (ANY ne porte pas de
  littéral, graphe de test corrigé), verts ensuite ; build complet vert

### Completion Notes List

- La correspondance nœud↔ligne est **reconstruite depuis les annotations
  `@id("uuid")`** du texte généré (regex, guillemets tolérés) — aucun changement de
  `core/script`, testée pur. Cliquer une ligne sans annotation remonte au nœud
  annoté le plus proche au-dessus.
- Bouton `Script` dans la barre (5.6b) : bascule la moitié droite ; le panneau de
  détails s'efface tant que la vue script est visible. Séparateur fixe 50/50
  (glissable = consigné avec le confort 5.7).
- Régénération débouncée 300 ms sur le même hook de mutation que la validation ;
  vue repliée = zéro régénération. `issues` du générateur affichées en tête.
- Sélection synchronisée dans les deux sens : nœud → ligne surlignée + défilement
  centré ; ligne → `focusNode` recentré dans la MOITIÉ canevas.
- **Importer** : deux clics (« Tout remplacer ? », U2) puis lecture du presse-papier,
  `replaceAll` = un geste d'annulation (UUID/positions conservés, méta et id de la
  session inchangés) ; le message rappelle que Ctrl+Z annule.
- Exporter réutilise `BlueprintFiles.export` (même chemin que /blueprint export).
- Coloration par classification de ligne (événement/var/commentaire) — les lexèmes
  fins attendront un besoin réel ; consigné.
- VERIFY-5.11 (rendu split, boutons) rejoint la session finale.

### File List

- `client/src/main/java/fr/blueprint/client/editor/ScriptViewState.java` (créé)
- `client/src/main/java/fr/blueprint/client/editor/ScriptView.java` (créé)
- `client/src/main/java/fr/blueprint/client/editor/CanvasController.java` (modifié — replaceAll)
- `client/src/main/java/fr/blueprint/client/editor/ToolbarWidget.java` (modifié — bouton Script)
- `client/src/main/java/fr/blueprint/client/editor/CanvasWidget.java` (modifié — intégration)
- `client/src/test/java/fr/blueprint/client/editor/ScriptViewStateTest.java` (créé)
- `core/src/main/resources/assets/blueprint/lang/{en_us,fr_fr}.json` (modifiés — 10 clés)

## QA Results

### Review Date: 2026-08-03 — Reviewed By: Quinn (Test Architect)

Gate: **PASS** → docs/qa/gates/5.11-vue-script.yml — SCRIPT-001 (medium : l'import laissait les anciens commentaires et perdait ceux du script) et SCRIPT-002 (surlignage périmé après régénération) corrigés en review. Story passée à Done.

# Stories 4.1 + 4.2 + 4.3 : BScript v1 — génération et parsing fidèles

## Status

Done

## Story

**En tant que** joueur et mainteneur,
**je veux** que tout graphe se génère en texte lisible et que ce texte se re-parse en graphe identique,
**afin de** versionner, diff-er, partager et éditer mes blueprints comme du code (FR23-FR25).

> Périmètre v1 assumé : sous-ensemble **annoté et total** de la grammaire de
> `bscript-spec.md` — appels nommés, expressions imbriquées de purs, blocs de branches
> uniformes (`type(args) { pin: { … } }`), étiquettes/goto pour les fusions et boucles,
> `@id`/`@pos` partout pour la fidélité. Le sucre (`if` nu, `wait 20t`, opérateurs
> infixes, `$pin` sans préfixe hors événement) = itération 4.2b sur cette base.

## Acceptance Criteria

1. **4.2 — Générateur** : `ScriptGenerator.generate(bp, lookup)` produit un texte **déterministe** (même graphe → mêmes octets, testé par double génération) : en-tête + meta, variables (types structurels `list<int>` inclus), notes, un bloc `on <event>(sorties…)` par nœud d'événement (triés par UUID), chaînes exec linéaires, purs **inlinés** en expressions (partagés dédupliqués par `@id`), branches en blocs par pin, fusions/boucles en `label:`/`goto` (repli spec §6), littéraux typés échappés.
2. **4.3 — Parseur** : `ScriptParser.parse(texte, registries)` reconstruit le graphe — erreur de syntaxe → résultat d'échec avec **ligne + message**, jamais d'exception ; nœud inconnu → **fantôme** (spec §7) ; `@id` absent → UUID généré ; `@pos` absent → mise en page automatique déterministe.
3. **Round-trip pivot** : démo réelle (7 nœuds, 2 événements, branche, purs) → texte → parse → `contentEquals` ; et `generate(parse(generate(g))) == generate(g)`.
4. Cas couverts en test : variables 4 portées + défauts + `list<int>`, boucle exec (goto), pur partagé entre deux consommateurs (un seul nœud au retour), notes, fantôme minimal, erreurs (ligne correcte, pas de crash).
5. **Câblage fichiers** : `BlueprintFiles.export` écrit désormais du **BScript texte** dans `.bp` ; `importFile` détecte le format (magie gzip → NBT hérité, sinon texte) — les `.bp` NBT existants restent importables.

## Tasks / Subtasks

- [x] `core/script/ScriptGenerator.java` (+ ordre déterministe, échappements, issues pour l'inemittable : orphelins, config non vide)
- [x] `core/script/ScriptParser.java` (lexer + descente récursive + construction directe du graphe + fantômes + auto-layout)
- [x] `BlueprintFiles` : export texte, import à détection de format ; ajustements tests 4.4a
- [x] `ScriptRoundTripTest` (déterminisme, démo, variables, goto, pur partagé, fantôme, erreurs)

## Change Log

| Date | Version | Description | Auteur |
|---|---|---|---|
| 2026-08-03 | 0.1 | Création groupée 4.1-4.3 (v1 fidèle annotée, sucre différé en 4.2b) | SM (Bob) |

## Dev Agent Record

### Agent Model Used

claude-fable-5 (James, dev)

### Debug Log References

- 5 échecs initiaux « nœud orphelin non émis » : les purs inlinés n'étaient jamais ajoutés
  à `emitted` — corrigé dans `renderSource` (branche d'inlinage).
- `variablesRoundTrip` : littéraux liste inemittables — ajout de la forme `[a, b, …]`
  des deux côtés (générateur : `renderRaw` récursif ; parseur : `Raw` bruts typés au
  point de réception par `convertRaw`, élément inféré pour les fantômes). `[` `]`
  ajoutés aux symboles du lexer.
- `outputIsReadable` : une branche à sortie unique câblée s'émettait linéairement —
  lisible comme inconditionnel. Linéarité restreinte aux nœuds à UNE sortie exec
  déclarée ; les branches restent toujours des blocs `pin: { … }`.
- Lexer : `.` retiré des caractères de mot (cassait `$node("uuid").pin`).

### Completion Notes List

- Générateur : texte déterministe (événements triés par UUID, args dans l'ordre du
  shape, extras triés), purs inlinés dédupliqués par `@id`, `label l_xxxxxxxx`/`goto`
  pour fusions et boucles, issues non bloquantes pour l'inemittable (orphelins, config).
- Parseur : descente récursive sur lexer maison, erreurs « ligne N : message » sans
  exception, nœuds inconnus → fantômes avec littéraux inférés du token, `@id` absent →
  UUID aléatoire, `@pos` absent → grille déterministe. Construction via `GraphLoader`
  (le chargement ne refuse rien — P4).
- `BlueprintFiles` : export = BScript texte ; import sniffe la magie gzip `0x1F 0x8B`
  → NBT hérité (4.4a), sinon parse texte. Signatures élargies à `LoadedRegistries`.
- 247 tests verts (8 nouveaux round-trip), build complet + JaCoCo OK.
- Différé en 4.2b : sucre (`if` nu, `wait 20t`, opérateurs infixes) ; littéraux map.

### File List

- core/src/main/java/fr/blueprint/core/script/ScriptGenerator.java (nouveau)
- core/src/main/java/fr/blueprint/core/script/ScriptParser.java (nouveau)
- core/src/main/java/fr/blueprint/core/graph/GraphLoader.java (nouveau)
- core/src/main/java/fr/blueprint/core/BlueprintFiles.java (export texte, import à détection)
- core/src/main/java/fr/blueprint/core/command/BlueprintCommand.java (passage LoadedRegistries)
- core/src/test/java/fr/blueprint/core/script/ScriptRoundTripTest.java (nouveau, 8 tests)
- core/src/test/java/fr/blueprint/core/DemoBlueprintTest.java (format texte + test NBT hérité)

## QA Results

### Review Date: 2026-08-03

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Générateur et parseur bien structurés : sortie déterministe vérifiée octet pour octet,
descente récursive lisible, erreurs toujours « ligne N : message » sans exception,
fantômes conformes à la spec §7, construction via GraphLoader (le chargement ne refuse
rien — P4). L'échappement des chaînes est sain (backslash traité en premier). Le Set de
liens du modèle neutralise les doublons au re-parse des purs partagés.

### Refactoring Performed (corrections en review, tests d'abord)

Quatre failles réelles trouvées par tests à risque — les quatre tests étaient rouges
avant correction :

- **SCRIPT-001 (high)** : `AddLink` n'efface pas le littéral du pin (repli style UE),
  mais `renderCall` n'émettait que le lien → le littéral de repli disparaissait au
  round-trip, dégradation **silencieuse** à chaque export/import. Corrigé : les deux
  arguments sont émis sur le même pin (littéral puis lien), le parseur applique dans
  l'ordre. `literalBehindLinkSurvivesRoundTrip`.
- **SCRIPT-002 (medium)** : littéraux/variables préservés en NBT brut (P4) perdus sans
  signalement à l'export texte. Corrigé : issue explicite « (P4) non émissibles en
  texte » (accesseurs publics `hasPreservedLiterals`/`hasPreservedVariables`). Le NBT
  monde reste le support total. Test dans `PreservationTest`.
- **SCRIPT-003 (medium)** : un script forgé (le chargement ne refuse rien) pouvait
  créer un cycle de purs ou un lien depuis un nœud absent → `StackOverflowError`/NPE à
  l'export. Corrigé : garde anti-réentrance d'inlinage et repli `$node` sur source
  absente — générateur total, fidélité conservée.
  `craftedPureCycleAndDanglingRefNeverCrashExport`.
- **SCRIPT-004 (low)** : étiquettes tronquées à 8 caractères d'UUID → collision
  possible, goto recâblé en silence. Corrigé : étiquettes séquentielles `l_1`, `l_2`…
  dans l'ordre déterministe de la pré-passe. `labelsStayDistinctWhenUuidPrefixesCollide`.

### Compliance Check

- Coding Standards : ✓
- Project Structure : ✓ (tout dans `core/script`, aucun contact avec `client/`)
- Testing Strategy : ✓ — 251 tests verts (12 sur BScript dont 4 ajoutés en review), build + JaCoCo OK
- All ACs Met : ✓ (AC1-AC5, sucre explicitement différé en 4.2b)

### Gate Status

Gate: **PASS** → docs/qa/gates/4.1-4.3-bscript-v1.yml

### Recommended Status

✓ **Done** — v2 recommandée plus tard : porter le NBT brut préservé en texte
(bloc `@preserved` base64) pour un P4 total hors NBT.

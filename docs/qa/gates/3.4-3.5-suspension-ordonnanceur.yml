# <!-- Powered by BMAD™ Core -->
schema: 1
story: "3.4-3.5"
story_title: "Suspension persistée et ordonnanceur"
gate: "PASS"
status_reason: "Le test pivot (wait à travers un redémarrage) tient, la police des budgets et l'équité sont prouvées. Deux corrections en review : la charge utile du déclencheur était perdue à la capture (medium), et une faute laissait tourner les exécutions sœurs d'un blueprint pourtant désactivé (low)."
reviewer: "Quinn (Test Architect)"
updated: "2026-08-02T21:46:20Z"
waiver: { active: false }
top_issues:
  - id: "SCHED-001"
    severity: medium
    finding: "RÉSOLU EN REVIEW — drainForSave capturait Map.of() comme charge utile du déclencheur : après un redémarrage, un nœud lisant trigger().output(...) aurait trouvé le vide, en contradiction avec l'AC1 (« l'événement déclencheur et sa charge utile »)"
    suggested_action: "Corrigé : TriggerContextImpl expose values(), la capture les emporte ; régression prouvant la traversée complète (capture → NBT → décodage) avec une charge utile réelle"
  - id: "SCHED-002"
    severity: low
    finding: "RÉSOLU EN REVIEW — une faute retirait la seule exécution fautive : les exécutions sœurs du même blueprint (pourtant désactivé par l'écouteur) continuaient de tourner jusqu'à terminaison"
    suggested_action: "Corrigé : purge des files prête et différée + garde d'itération ; régression : deux exécutions jumelles, une seule notification, activeCount 0"
risk_summary:
  totals: { critical: 0, high: 0, medium: 1, low: 1 }
  recommendations:
    must_fix: []
    monitor:
      - "La reprise avec références RÉSOLUES (joueur présent) exige un ServerPlayer réel : gametest 1.6/6.1 (assumé dans l'AC3)"
      - "drainForSave ignore les valeurs non sérialisables (exécution perdue avec avertissement) — comportement assumé, à rappeler dans la doc utilisateur (9.5)"
